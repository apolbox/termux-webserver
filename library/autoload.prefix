#!/usr/bin/bash
#
# Autoload file in library
#
WEB_SERVER_DIR="/data/data/com.termux/files/usr/etc/lampp"

# Memuat semua file dari library
listfile=($WEB_SERVER_DIR/library/functions/*)

for i in ${!listfile[@]}
do
    source ${listfile[$i]}
done

